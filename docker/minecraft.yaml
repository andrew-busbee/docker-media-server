version: "3.3"

services:
  minecraft:
    image: itzg/minecraft-server
    ports:
      - $PORTS
    environment:
      EULA: "TRUE"
      SERVER_NAME: $SERVERNAME
      MOTD: $MOTD
      DIFFICULTY: $DIFFICULTY
      # https://github.com/itzg/docker-minecraft-server#memory-limit
      MEMORY: $MEMORY
      # uncomment this to import a saved game. (put your savegame directory in the saves-to-import directory)
      #WORLD: $WORLD

    volumes:
      - $DOCKERDIR/minecraft-data:/data
      - $DOCKERDIR/saves-to-import:/worlds:ro
